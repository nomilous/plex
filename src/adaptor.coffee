class Adaptor 

    #
    # base literal for an Adaptor
    # 
    # ie. The thing that listens for 'clients'
    #     attempting to attach, as edges, onto
    #     the local node.
    #
    
    @listen: ( opts = {}, onConnect ) -> 


module.exports = Adaptor
