// Generated by CoffeeScript 1.4.0
var Tree;

Tree = (function() {

  function Tree(context) {
    this.context = context;
    this.edges = {};
  }

  Tree.prototype.insert = function(localEdge, connectData) {
    var id;
    id = localEdge.localId();
    this.edges[id] = {
      local: {
        globalId: localEdge.globalId(),
        mode: this.context.mode
      },
      remote: connectData
    };
    return this.context.uplink.send('event:edge:connect', this.edges[id]);
  };

  return Tree;

})();

module.exports = Tree;
