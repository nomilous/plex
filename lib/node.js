// Generated by CoffeeScript 1.4.0
var Base;

Base = (function() {

  function Base() {}

  Base.edges = {};

  Base.start = function(opts) {
    var adaptor, port, server;
    if (opts == null) {
      opts = {};
    }
    adaptor = opts.adaptor || 'socket.io';
    port = process.env.PORT || opts.port || 3001;
    switch (adaptor) {
      case 'socket.io':
        server = require("./adaptors/" + adaptor + "-adaptor");
        return server.listen(opts, this.connect);
      default:
        throw "adaptor not implemented: " + adaptor;
    }
  };

  Base.connect = function(edge) {
    return this.edges[edge.localId()] = edge;
  };

  Base.disconnect = function(edge) {
    return this.edges[edge.localId()].disconnected = {
      timestamp: new Date()
    };
  };

  return Base;

})();

module.exports = Base;
