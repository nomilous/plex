// Generated by CoffeeScript 1.4.0
var Edge, os;

os = require('os');

Edge = (function() {

  function Edge(connection, opts) {
    this.connection = connection;
    this.opts = opts != null ? opts : {};
    if (this.connection) {
      this.connection._edge = this;
    }
  }

  Edge.prototype.connect = function(onConnect) {
    var connectedThing;
    connectedThing = {
      id: 'LOCAL_ID'
    };
    this.connection = connectedThing;
    this.handshake();
    if (onConnect instanceof Function) {
      return onConnect(this);
    }
  };

  Edge.prototype.handshake = function() {
    return this.send('event:connect', {
      type: this.opts.mode,
      globalId: this.globalId()
    });
  };

  Edge.prototype.send = function(event, payload) {
    return this.sentAmessage = {
      event: event,
      payload: payload
    };
  };

  Edge.prototype.localId = function() {
    if (!this.connection) {
      return 'LOCAL_ID';
    }
    return this.connection.id;
  };

  Edge.prototype.globalId = function() {
    return os.hostname() + '%' + process.pid;
  };

  return Edge;

})();

module.exports = Edge;
