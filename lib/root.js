// Generated by CoffeeScript 1.4.0
var root;

root = {
  edges: {},
  start: function(opts) {
    var port, transport;
    if (opts == null) {
      opts = {};
    }
    transport = opts.transport || 'socket.io';
    port = process.env.PORT || opts.port || 3001;
    switch (transport) {
      case 'socket.io':
        return console.log('start');
      default:
        throw "transport not implemented: " + transport;
    }
  },
  connect: function(edge) {
    return root.edges[edge.localId()] = edge;
  },
  disconnect: function(edge) {
    return root.edges[edge.localId()].disconnected = {
      timestamp: new Date()
    };
  }
};

module.exports = root;
