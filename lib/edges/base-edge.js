// Generated by CoffeeScript 1.4.0
var BaseEdge, os;

os = require('os');

BaseEdge = (function() {

  function BaseEdge(connection, context) {
    this.connection = connection;
    this.context = context;
    console.log('\nBaseEdge() with:', this.context);
    if (this.connection) {

    } else {
      this.validate(this.context);
    }
  }

  BaseEdge.prototype.validate = function(context) {
    if (!context) {
      throw 'undefined context';
    }
    if (!(context.connnect && context.connect.adaptor)) {
      throw 'edge requires connect.adaptor';
    }
  };

  return BaseEdge;

})();

module.exports = BaseEdge;
